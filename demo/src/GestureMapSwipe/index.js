import React from 'react'
import PropTypes from 'prop-types'

const GestureMapSwipe = ({}) => {
  return (<div>GestureMapSwipe</div>)
}

GestureMapSwipe.defaultProps = {
}
GestureMapSwipe.propTypes = {
}

export default GestureMapSwipe
