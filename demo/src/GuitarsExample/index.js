import React, { Component } from 'react'
import PropTypes from 'prop-types'

class GuitarExample extends Component {
  static defaultProps = {
  }

  static propTypes = {
  }

  state = {
  }

   = () => {
  }

  render() {
    return (<div>GuitarExample</div>)
  }
}

export default GuitarExample
